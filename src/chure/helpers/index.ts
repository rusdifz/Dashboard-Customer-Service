import {
    ErrorHandler,
    handleError
} from './error';

const helperServices = Object.freeze({
    ErrorHandler,
    handleError
})

export default helperServices;
export {
    ErrorHandler,
    handleError
}